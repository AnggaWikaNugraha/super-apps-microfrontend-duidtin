/** DOM tiruan: paket ini menyentuh window, localStorage, dan event storage. */
import { GlobalRegistrator } from "@happy-dom/global-registrator";

GlobalRegistrator.register({ url: "http://localhost:3000" });
