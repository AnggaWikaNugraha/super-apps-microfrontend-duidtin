/** Nama container design-system — harus sama persis dengan `name` di config MF-nya. */
export const DESIGN_SYSTEM_REMOTE = "duidtin_ui_design_system";

/** Path remoteEntry design-system, relatif terhadap base URL federation. */
export const DESIGN_SYSTEM_ENTRY_PATH = "/design-system/static/remoteEntry.js";

/** Port dev design-system, dipakai kalau repo ini dibuka sendiri di localhost. */
export const LOCAL_DESIGN_SYSTEM_ORIGIN = "http://localhost:3001";
