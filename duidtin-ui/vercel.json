{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "sha=\"${VERCEL_GIT_PREVIOUS_SHA:-HEAD^}\"; git cat-file -e \"$sha^{commit}\" 2>/dev/null || exit 1; git diff --quiet \"$sha\" HEAD -- . ../duidtin-packages/auth && exit 0 || exit 1"
}
