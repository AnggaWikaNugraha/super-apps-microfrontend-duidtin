import { List, Panel, Root, Tab } from "./root";

export const Tabs = Object.assign(Root, {
  Root,
  List,
  Tab,
  Panel,
});

export type { TabsListProps, TabsPanelProps, TabsRootProps, TabsTabProps } from "../../types/tabs/tabs.types";
