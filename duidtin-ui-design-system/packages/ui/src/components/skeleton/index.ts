import { Lines, Root } from "./root";

export const Skeleton = Object.assign(Root, {
  Root,
  Lines,
});

export type { SkeletonLinesProps, SkeletonRootProps, SkeletonVariants } from "../../types/skeleton/skeleton.types";
