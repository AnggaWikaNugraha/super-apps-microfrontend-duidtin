/** Nama container design-system, sama persis dengan `name` di rslib.config.ts-nya. */
export const DESIGN_SYSTEM_REMOTE = "duidtin_ui_design_system";

/** Path remoteEntry design-system — sama di semua environment, yang beda base URL-nya. */
export const DESIGN_SYSTEM_ENTRY_PATH = "/design-system/static/remoteEntry.js";

/** Dev lokal: design-system jalan di port sendiri, jadi beda origin. */
export const LOCAL_DESIGN_SYSTEM_ORIGIN = "http://localhost:3001";
